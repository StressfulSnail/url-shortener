spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
        hibernate:
            show_sql: true
            format_sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/url_shortener
    username: root
    password: toor

logging:
    level:
        org:
            hibernate:
                type: trace
